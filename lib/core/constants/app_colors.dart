import 'package:flutter/material.dart';

class AppColors {
	static const Color primary = Color(0xFF006400); // أخضر داكن
	static const Color accent = Color(0xFFB71C1C); // أحمر داكن
	static const Color background = Color(0xFFF5F5F5); // خلفية فاتحة
	static const Color text = Color(0xFF212121); // نص داكن
	static const Color error = Color(0xFFD32F2F); // لون الخطأ
	static const Color success = Color(0xFF388E3C); // لون النجاح
}
